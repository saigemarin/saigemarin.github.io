//abandoned after so many attempts to make work, 
// going to try pivoting to key pressed

// function mouseClicked() {
//   console.log(mouseX, mouseY)

//   // let inside = isInsideCheckbox (myCheckBox1, 
//   //   mouseX, mouseY)

//   let inside1 = 
//     myCheckBox1.x < mouseX && 
//     mouseX < myCheckBox1.x + myCheckBox1.width &&
//     myCheckBox1.y < mouseY 
//     && mouseY < myCheckBox1.y + myCheckBox1.height
  
//   console.log(inside1)
  
//   if(inside1){
//     //using console log to test if this is working since 
//     //its not drawing the burger the way it should.
//     console.log('hello')

//     // burgerBun1.display ()
//   } else {}

// }

//other attempt

// //below is the original attempt at the clicking 
// // inside the checkboxes, 
// // this is based on the example we did in class.

// // function isInsideCheckbox(obj, x, y) {
// // if(myCheckBox1.x < x && 
// //     myCheckBox1.x + myCheckBox1.width > x &&
// //     myCheckBox1.y < y &&
// //     myCheckBox1.y + myCheckBox1.height > y){
// //         return true}
// //     else {
// //         return false}
// // }