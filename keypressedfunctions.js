function keyTyped(){
    if (key === 'a') {
        console.log ('hello')

        push()
        burgerBun1.display ()
        pop()

        push()
        checkBoxes1.colorChange ()
        pop()
    } else {}
}